# CB-3 Interactive Editor Fix

The landing page builder now uses explicit mobile section editing. Selecting a section opens a bottom-sheet editor instead of relying on an inline editor rendered below the section list.

The editor supports direct editing of the current builder block types and section-level controls while preserving the shared versioned builder document.
